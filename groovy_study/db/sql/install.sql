CREATE TABLE TEST(
	ID INTEGER PRIMARY KEY,
	FIRSTNAME VARCHAR(20),
	NAME VARCHAR(50),
	ZIP INTEGER
);

-------------------
INSERT INTO TEST
values (1,'visual','khh',5);

---------------------

select * from TEST;
